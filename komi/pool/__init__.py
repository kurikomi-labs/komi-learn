"""The Global Learnings pool: contribution (scrub→sign→queue→outbox) and
consumption (pull→verify→cache). Network is stubbed to a local outbox/inbox in
v1; the same record format is what a future pool server would accept."""
